services:
  safekey:
    build: .
    restart: unless-stopped
    ports:
      - "8787:8787"
    environment:
      PORT: 8787
      NODE_ENV: production
    volumes:
      - safekey-data:/app/data

volumes:
  safekey-data:
